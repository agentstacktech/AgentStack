# Synergy: webhook → logic + scheduler

**Domains:** `webhooks.*`, `logic.*`, `scheduler.*`

Register inbound webhooks, attach Logic Engine rules for validation/enrichment, and schedule follow-up tasks with `scheduler.create_task`.

See [logic/LOGIC_ENGINE_V2.md](../../logic/LOGIC_ENGINE_V2.md) and [scheduler/SCHEDULER_QUICKSTART.md](../../scheduler/SCHEDULER_QUICKSTART.md).
