# Synergy: Compass + capability tasks

**Domains:** `frontend.platform.compass`, capability task manifests

Platform Compass (Cmd+K) deep-links into comfort task hosts declared in capability task manifests — same action ids as hub tiles.

See [platform/CAPABILITY_TASKS.md](../../platform/CAPABILITY_TASKS.md).
