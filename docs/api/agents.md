# Agents API — overview

| Surface | Notes |
|---------|--------|
| REST | `GET /api/projects/{projectId}/agents`, `GET /api/users/me/agents`, run/status routes — see Swagger **agents** tag |
| MCP | `agents.*` — [MCP_CAPABILITY_MATRIX.md](../MCP_CAPABILITY_MATRIX.md) |

Full integration walkthrough: [agents/INTEGRATION_QUICKSTART.md](../agents/INTEGRATION_QUICKSTART.md).
